<Window x:Class="InazumaElevenVRSaveEditor.Features.MemoryEditor.Views.ItemListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Working on Items"
        Height="600"
        Width="800"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Working on These Items"
                     Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                     Margin="0,0,0,10"/>
            <TextBlock Text="These are the items currently being worked on in the memory editor"
                     Style="{StaticResource MaterialDesignBody1TextBlock}"
                     Opacity="0.7"
                     TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Items List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Width="220"
                                           Height="220"
                                           Margin="10"
                                           UniformCornerRadius="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Item Image -->
                                <Border Grid.Row="0"
                                      Background="White"
                                      Padding="20">
                                    <Image Source="{Binding ImagePath}"
                                         Stretch="Uniform"
                                         RenderOptions.BitmapScalingMode="HighQuality"/>
                                </Border>

                                <!-- Item Info -->
                                <Border Grid.Row="1"
                                      Background="{DynamicResource PrimaryHueMidBrush}"
                                      Padding="15,10">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}"
                                                 FontSize="16"
                                                 FontWeight="Bold"
                                                 Foreground="White"
                                                 TextWrapping="Wrap"
                                                 HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding Category}"
                                                 FontSize="12"
                                                 Foreground="White"
                                                 Opacity="0.8"
                                                 Margin="0,5,0,0"
                                                 HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
