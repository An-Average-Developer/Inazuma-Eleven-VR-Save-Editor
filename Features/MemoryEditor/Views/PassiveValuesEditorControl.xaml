<UserControl x:Class="InazumaElevenVRSaveEditor.Features.MemoryEditor.Views.PassiveValuesEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:Card Grid.Row="0" Style="{StaticResource CardStyle}">
            <StackPanel>
                <Button Command="{Binding MemoryEditor.BackToMenuCommand}"
                      Style="{StaticResource MaterialDesignOutlinedButton}"
                      HorizontalAlignment="Left"
                      Margin="0,0,0,15"
                      Height="35">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ChevronLeft"
                                               VerticalAlignment="Center"
                                               Margin="0,0,8,0"/>
                        <TextBlock Text="Back to Menu" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <TextBlock Text="Passive Values Editor"
                         Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                         Margin="0,0,0,15"/>

                <TextBlock Text="Edit passive values in real-time when hovering over passives in the Abilearn Board. Make sure the game is running and you are attached to the process."
                         Style="{StaticResource MaterialDesignBody2TextBlock}"
                         Opacity="0.7"
                         TextWrapping="Wrap"
                         Margin="0,0,0,10"/>
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="1" Style="{StaticResource CardStyle}" Margin="10">
            <StackPanel>
                <Border Background="{DynamicResource MaterialDesignDivider}"
                       CornerRadius="8"
                       Margin="0,0,0,20">
                    <Grid Margin="15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Image Grid.Column="0"
                               Source="/Resources/Cards/Pasivas.png"
                               Width="32"
                               Height="32"
                               VerticalAlignment="Center"
                               Margin="0,0,15,0"
                               RenderOptions.BitmapScalingMode="HighQuality"/>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Current Status"
                                     FontSize="12"
                                     Opacity="0.7"/>
                            <TextBlock Text="{Binding MemoryEditor.StatusMessage}"
                                     FontSize="24"
                                     FontWeight="Bold"
                                     Margin="0,5,0,0"/>
                            <TextBlock Text="Click attach to connect to the game"
                                     FontSize="11"
                                     Opacity="0.6"
                                     Margin="0,5,0,0"
                                     FontStyle="Italic"/>
                        </StackPanel>

                        <Button Grid.Column="2"
                              Command="{Binding MemoryEditor.AttachToProcessCommand}"
                              Style="{StaticResource MaterialDesignRaisedButton}"
                              Height="45"
                              VerticalAlignment="Center"
                              Background="#4CAF50">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Link"
                                                       Width="20"
                                                       Height="20"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"/>
                                <TextBlock Text="Attach to Game"
                                         VerticalAlignment="Center"
                                         FontWeight="Medium"/>
                            </StackPanel>
                        </Button>

                        <Button Grid.Column="2"
                              Command="{Binding MemoryEditor.DetachFromProcessCommand}"
                              Style="{StaticResource MaterialDesignRaisedButton}"
                              Height="45"
                              VerticalAlignment="Center"
                              Background="#D32F2F"
                              Visibility="{Binding MemoryEditor.IsAttached, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="LinkOff"
                                                       Width="20"
                                                       Height="20"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"/>
                                <TextBlock Text="Detach"
                                         VerticalAlignment="Center"
                                         FontWeight="Medium"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </Border>

                <!-- Enable Passive Value Editing Toggle -->
                <Border Background="#3F51B5"
                       CornerRadius="8"
                       Margin="0,20,0,0"
                       Visibility="{Binding MemoryEditor.IsAttached, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid Margin="15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <materialDesign:PackIcon Grid.Column="0"
                                               Kind="Pencil"
                                               Width="28"
                                               Height="28"
                                               VerticalAlignment="Center"
                                               Margin="0,0,15,0"
                                               Foreground="White"/>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Enable Passive Value Editing"
                                     FontSize="16"
                                     FontWeight="Bold"
                                     Foreground="White"/>
                            <TextBlock Text="Inject code to edit passive values when hovering in Abilearn Board"
                                     FontSize="11"
                                     Foreground="White"
                                     Opacity="0.9"
                                     Margin="0,3,0,0"/>
                        </StackPanel>

                        <ToggleButton Grid.Column="2"
                                     IsChecked="{Binding MemoryEditor.IsPassiveValueEditingEnabled, Mode=OneWay}"
                                     Style="{StaticResource MaterialDesignSwitchToggleButton}"
                                     ToolTip="Toggle passive value editing on/off"
                                     Command="{Binding MemoryEditor.TogglePassiveValueEditingCommand}"
                                     VerticalAlignment="Center"
                                     Margin="10,0,0,0"/>
                    </Grid>
                </Border>

                <!-- Content Area -->
                <Grid Margin="0,20,0,0" Visibility="{Binding MemoryEditor.IsPassiveValueEditingEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <materialDesign:Card Style="{StaticResource CardStyle}">
                        <Grid>
                            <StackPanel>
                                <TextBlock Text="Passive Value Editor"
                                         Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                         Margin="0,0,0,15"/>

                                <TextBlock Text="Hover over a passive in the Abilearn Board to edit its value. The passive value will appear below."
                                         Style="{StaticResource MaterialDesignBody2TextBlock}"
                                         Opacity="0.7"
                                         TextWrapping="Wrap"
                                         Margin="0,0,0,20"/>

                                <!-- Current Passive Info -->
                                <Border Background="{DynamicResource MaterialDesignDivider}"
                                       CornerRadius="8"
                                       Padding="15"
                                       Margin="0,0,0,20">
                                    <StackPanel>
                                        <TextBlock Text="Current Passive"
                                                 FontSize="14"
                                                 FontWeight="SemiBold"
                                                 Margin="0,0,0,10"/>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Grid.Row="0" Grid.Column="0"
                                                     Text="Value Type:"
                                                     FontWeight="SemiBold"
                                                     Margin="0,0,10,5"/>
                                            <TextBlock Grid.Row="0" Grid.Column="1"
                                                     Text="{Binding MemoryEditor.PassiveValueType}"
                                                     Margin="0,0,0,5"/>

                                            <TextBlock Grid.Row="1" Grid.Column="0"
                                                     Text="Current Value:"
                                                     FontWeight="SemiBold"
                                                     Margin="0,0,10,0"/>
                                            <TextBlock Grid.Row="1" Grid.Column="1"
                                                     Text="{Binding MemoryEditor.PassiveCurrentValue}"
                                                     FontSize="18"
                                                     FontWeight="Bold"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <!-- New Value Input -->
                                <StackPanel Visibility="{Binding MemoryEditor.HasPassiveValue, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Border Background="White"
                                           CornerRadius="8"
                                           Padding="8,4"
                                           HorizontalAlignment="Left"
                                           Margin="0,0,0,10">
                                        <TextBlock Text="New Value"
                                                 FontSize="14"
                                                 FontWeight="SemiBold"
                                                 Foreground="Gray"/>
                                    </Border>

                                    <TextBox Text="{Binding MemoryEditor.PassiveNewValue, UpdateSourceTrigger=PropertyChanged}"
                                           Background="White"
                                           BorderBrush="Gray"
                                           BorderThickness="2"
                                           Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                           FontSize="24"
                                           Margin="0,0,0,20"
                                           Padding="15"/>

                                    <!-- Apply Button -->
                                    <Button Command="{Binding MemoryEditor.ApplyPassiveValueCommand}"
                                          Style="{StaticResource MaterialDesignRaisedButton}"
                                          Height="50">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="Check"
                                                                   Width="24"
                                                                   Height="24"
                                                                   VerticalAlignment="Center"
                                                                   Margin="0,0,10,0"/>
                                            <TextBlock Text="Apply New Passive Value"
                                                     VerticalAlignment="Center"
                                                     FontSize="16"
                                                     FontWeight="Medium"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>

                                <!-- No Passive Selected Placeholder -->
                                <StackPanel Visibility="{Binding MemoryEditor.HasPassiveValue, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                                          HorizontalAlignment="Center"
                                          Margin="0,40,0,0">
                                    <materialDesign:PackIcon Kind="CursorPointer"
                                                           Width="64"
                                                           Height="64"
                                                           HorizontalAlignment="Center"
                                                           Opacity="0.3"
                                                           Margin="0,0,0,20"/>
                                    <TextBlock Text="Hover over a passive in the game"
                                             Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                             Opacity="0.5"
                                             HorizontalAlignment="Center"/>
                                    <TextBlock Text="The passive value will appear here for editing"
                                             Style="{StaticResource MaterialDesignBody2TextBlock}"
                                             Opacity="0.4"
                                             HorizontalAlignment="Center"
                                             Margin="0,10,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </materialDesign:Card>
                </Grid>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
